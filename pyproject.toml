[project]
name = "sentinel-hound"
version = "1.0.0"
description = "Local-first log threat detection + SOC triage toolkit"
requires-python = ">=3.9"

[project.scripts]
sentinel-hound = "sentinel_hound.cli:main"
